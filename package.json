{
  "name": "scouting-app",
  "version": "0.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "predev": "mongod --replSet rs0 --dbpath ./data > data/logfile &",
    "dev": "trap 'exit 0' SIGINT; npm run dev --workspace=packages/backend",
    "postdev": "mongo admin --eval 'db.shutdownServer()' > /dev/null",
    "start": "nodemon packages/backend/server.ts",
    "build": "NODE_ENV=production webpack --config ./webpack.production.config.js"
  },
  "author": "Alex Waese-Perlman",
  "license": "MIT",
  "workspaces": [ "packages/backend", "packages/web", "packages/shared" ],
  "private": true
}
